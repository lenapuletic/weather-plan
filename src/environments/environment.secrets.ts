/**
 * Stub so TypeScript and the IDE can resolve `./environment.secrets`.
 * `npm run build` / `npm run deploy` run `scripts/generate-secrets.mjs`, which
 * overwrites this file with the real OpenWeather key from OPENWEATHER_API_KEY.
 *
 */
export const openWeatherApiKey = '';
